{
  "name": "webgpu-galaxy-merger",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --config vitest.config.ts",
    "test:galaxy": "vitest run --config vitest.galaxy.config.ts",
    "dev:galaxy-test": "node scripts/galaxy-test-dev.mjs",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^7.2.1"
  },
  "devDependencies": {
    "@webgpu/types": "^0.1.71",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
